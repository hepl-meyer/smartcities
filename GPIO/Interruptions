#Programme interruption
# baptiste.meyer@student.hepl.be
# 9/2/2023

import machine
import utime

LED = machine.Pin(16,machine.Pin.OUT)

"""
#Ce code configure une boucle for qui allume et éteint une LED 10 fois.
#La boucle commence par une instruction for qui définit le nombre de fois que la boucle doit s'exécuter (10 fois).
#Pour chaque exécution, la LED s'allume pendant une seconde, puis s'éteint pendant une seconde.
for i in range(10):
    LED.value(1)
    utime.sleep(1)
    LED.value(0)
    utime.sleep(1)

#Ce code configure une boucle while true qui allume et éteint une LED en boucle.
#La boucle commence par une condition while true qui signifie que la boucle s'exécutera indéfiniment.
#Chaque fois que la boucle s'exécute, elle allume la LED pendant une seconde et l'éteint pendant une seconde.
#La boucle s'exécute donc en boucle, ce qui signifie que la LED clignote en permanence.
while True :
    LED.value(1)
    utime.sleep(1)
    LED.value(0)
    utime.sleep(1)
"""
